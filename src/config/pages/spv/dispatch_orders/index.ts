const page = {
  title: "dispatch_orders",
  permission: "view spv dispatch orders",
  createPath: {permission:"edit spv dispatch orders", path:"/spv/dispatch_orders/create"},
  breadcrumb: [
    {
      name: "home",
      path: "/home",
    }
  ],
};

export default page;
