const page = {
  title: "dispatch_orders",
  permission: "view dispatch orders",
  createPath: {permission:"edit dispatch orders", path:"/factory/dispatch_orders/create"},
  breadcrumb: [
    {
      name: "home",
      path: "/home",
    }
  ],
};

export default page;
