const page = {
  title: "types",
  permission: "view factory types",
  createPath: {permission:"edit factory types", path:"/factory/settings/types/create"},
  breadcrumb: [
    {
      name: "home",
      path: "/home",
    }
  ],
};

export default page;
